const authController = require('./auth');
const themeController = require('./themeController');
const postController = require('./postController');
const articleControler = require('./articlesController')

module.exports = {
    authController,
    themeController,
    postController,
    articleControler
}